services:
  - type: web
    name: my-tracker-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    envVars:
      - key: BOT_TOKEN
        sync: false
    plan: free
    autoDeploy: true
